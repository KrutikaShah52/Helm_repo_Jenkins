pipeline {
  agent {
    kubernetes {
      label 'helm-pod'
      containerTemplate {
        name 'helm'
        image 'wardviaene/helm-s3'
        ttyEnabled true
        command 'cat'
      }
    }
  }
  stages {
    stage('Run helm') {
      steps {
        container('helm-pod') {
          git url: 'git://github.com/KrutikaShah52/Helm_repo_Jenkins.git', branch: 'master'
          sh '''
             echo 'HEllooo'
         '''
          }
        }
      }
  }
}


